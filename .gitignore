# ---- Secrets & local config (never commit) ----
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/
# Virtual environments
.venv/
venv/
env/
# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
# uv
.uv/

# ---- Node / React UI ----
node_modules/
ui/dist/
ui/build/
ui/.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Docker / data volumes ----
# Local Postgres/Redis data if mounted into the repo
data/
pgdata/
*.pid
# Local-only compose overrides (dev secrets, ports) — never commit
docker-compose.override.yml
docker-compose.override.yaml

# ---- Logs & temp ----
*.log
logs/
tmp/
.cache/

# ---- OS / editor cruft ----
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# ---- Claude Code ----
# Keep shared project config & commands; ignore personal local overrides
.claude/settings.local.json
.claude/scheduled_tasks.lock
sprint/
docs/
demo/
.claude/